# 이 프로젝트에서 작업할 때의 지침

작업을 시작할 때 `docs/HANDOFF.md`를 읽어 현재 상태와 사용자 결정을 확인한다. 앱 배포, 공개 링크 전달, GitHub Pages 수정 작업은 `docs/DEPLOYMENT.md`도 읽는다. 사용자가 이후 명시한 요청은 이 문서보다 우선한다.

## 앱 배포와 링크 전달

- 사용자가 “앱을 보여줘”, “페이지를 열어줘”, “배포해줘”라고 하면 실제로 실행되는 앱을 제공한다. 저장소 페이지, README, 개발 문서, 로컬 미리보기 주소를 공개 앱 주소로 대신 전달하지 않는다.
- 이 프로젝트의 공개 앱 주소는 `https://jeoungan.github.io/Visitor-s_book/`이다. 주소는 설정을 바꿀 때 다시 확인한다. GitHub에 소스를 푸시한 사실만으로 배포 완료라고 말하지 않는다.
- 저장소 루트에는 앱 진입 파일이 없다. 원본 앱은 `public/index.html`이다. Pages가 저장소 루트의 README를 사이트로 표시하도록 `main`의 `/`를 그대로 게시하지 않는다. 정적 빌드 결과를 GitHub Actions의 Pages artifact로 배포한다.
- 배포 artifact 루트에 실행 가능한 `index.html`, JavaScript, CSS와 필요한 이미지가 있어야 한다. 프로젝트 경로 `/Visitor-s_book/`와 `/Visitor-s_book/guestbook/` 진입을 모두 검증한다. 루트 절대 경로 `/assets/...`, `/app.js` 때문에 파일이 잘못 요청되지 않도록 한다.
- 실제 방명록 DB, 환경 변수 비밀값, 로그, 내부 개발 문서, 이미지 생성 원본과 검토 산출물은 공개 artifact에 넣지 않는다. 배포에 필요한 완성 이미지와 manifest는 빠뜨리지 않는다.
- GitHub Pages는 Node 서버와 SQLite를 실행하지 않는다. Pages에서 브라우저 저장소를 사용하는 기능은 해당 브라우저에만 저장되며, 다른 방문자와 공유되는 방명록이라고 설명하지 않는다. 공유 방명록이 필요하면 별도의 실행 서버와 영구 저장소를 연결하고 다중 브라우저 검증을 마친다.
- 사용자의 배포 요청은 해당 프로젝트의 배포 작업 범위에서 수행한다. 이를 이유로 저장소 공개 범위나 계정 전체의 정책을 임의로 변경하지 않는다.

## 배포 완료의 기준

다음 조건을 확인한 뒤에만 배포 완료라고 보고한다.

1. 배포하려는 커밋과 원격 `main`을 확인하고, 그 커밋에 해당하는 검사 및 Pages 배포 작업이 성공했다.
2. 실제 공개 URL에 HTTP로 접속했을 때 문서가 아닌 앱 HTML이 반환되고, JavaScript·CSS·이미지가 정상 응답한다.
3. 실제 공개 URL을 브라우저에서 열어 예식장 canvas와 캐릭터가 로딩되는 모습을 확인했다. 로컬 성공이나 HTTP 200만으로 이 조건을 대체하지 않는다.
4. 모바일과 데스크톱에서 화면 비율, 주요 버튼과 이동 등 실제 상호작용을 확인하고, 콘솔 오류와 실패한 자산 요청을 확인했다.
5. 새로고침과 `/guestbook/` 직접 진입에서도 앱이 열리며, 배포된 코드·이미지의 캐시 버전이 현재 수정 내용과 맞는다.

배포 대기, 권한 오류 또는 검증 실패가 남으면 현재 상태와 남은 작업을 정확히 기록한다. 완료를 미리 주장하지 않는다. 최종 답변에는 검증한 공개 앱 링크를 먼저 전달하고, 저장 방식의 한계가 있으면 함께 설명한다.

## 데이터 보존과 검증

- 실제 데이터가 있는 `data/garden.sqlite`를 테스트로 수정하거나 초기화하지 않는다. 쓰기 검증에는 임시 DB 또는 별도 fixture를 사용한다.
- Node 서버 모드와 Pages 모드의 저장 동작을 구분한다. Pages 대응 때문에 기존 서버의 저장·수정·삭제 기능을 깨뜨리지 않는다.
- 코드에 맞는 문법 검사와 의미 있는 자동 테스트를 실행하고, 화면 변경은 실제 브라우저에서 확인한다. 이 프로젝트의 기본 검사 명령은 `npm run check`와 `npm test`이다.
- 배포 방식이나 공개 URL을 바꾸면 `docs/DEPLOYMENT.md`와 `docs/HANDOFF.md`를 함께 갱신한다. 배포 성공을 기록할 때는 커밋과 검증한 URL을 남긴다.
